spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crunchy_practice
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /api